//Declaring API version.
const API_Version = 'V1';

//IP Server.
const IP_Server = 'localhost';

//DataBase port.
const portDB = 27017;

//Database name
const dbName = 'flex';

//This is the way we export modules on NodeJs.
module.exports = {
    API_Version,
    IP_Server,
    portDB,
    dbName
}